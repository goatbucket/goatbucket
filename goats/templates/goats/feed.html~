<ul>
{% for event in event_list %}
    <h2>{{ event.expiration|date:"DATE_FORMAT" }}</h2>
    <p>{{event}}</p>
    <form action="{% url 'views.hide' event.id %}" method="post">
    {% csrf_token %}
    <input type="submit" id="{{event.id }}" value="Vote" />
    </form>    
{% empty %}
    <li>No upcoming events for you.</li>
{% endfor %}
</ul>

